pluginManagement {
    repositories {
        mavenCentral()
        gradlePluginPortal()
        // 添加intellij插件仓库
        maven { url 'https://plugins.jetbrains.com/maven' }
    }
}

rootProject.name = 'pre-submit-code-review' 